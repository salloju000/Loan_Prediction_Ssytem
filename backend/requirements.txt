# Backend dependencies for Loan Prediction API
# Python 3.12+
#
# Install:  pip install -r requirements.txt
# Run:      uvicorn main:app --reload --port 8000

# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.129.0,<1.0.0
uvicorn[standard]>=0.40.0,<1.0.0
pydantic>=2.12.0,<3.0.0
slowapi>=0.1.9,<1.0.0

# ── ML / Data ────────────────────────────────────────────────────────────────
scikit-learn==1.6.1
numpy>=2.4.0,<3.0.0
pandas>=3.0.0,<4.0.0
